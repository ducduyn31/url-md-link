{
  "name": "give-me-a-link",
  "version": "1.5.0",
  "private": true,
  "description": "Copy the current page as a Markdown link to the clipboard.",
  "license": "MIT",
  "scripts": {
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "typecheck": "tsc",
    "test": "bun test",
    "dev": "bun scripts/dev.ts",
    "build": "bun scripts/build.ts",
    "package": "bun scripts/package.ts",
    "verify": "bun run lint && bun run format:check && bun run typecheck && bun test",
    "changeset": "changeset",
    "version": "changeset version && bun scripts/sync-manifest-version.ts && bun run format",
    "release": "bun scripts/release.ts"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@happy-dom/global-registrator": "^20.11.2",
    "@tailwindcss/cli": "^4.3.3",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/preact": "^3.2.4",
    "@types/bun": "^1.3.14",
    "@types/chrome": "^0.2.5",
    "clsx": "^2.1.1",
    "oxfmt": "^0.63.0",
    "oxlint": "^1.78.0",
    "preact": "^10.29.8",
    "react-hook-form": "^7.85.0",
    "tailwind-merge": "^3.6.0",
    "tailwindcss": "^4.3.3",
    "typescript": "^6.0.3",
    "usehooks-ts": "^3.1.1"
  },
  "engines": {
    "bun": ">=1.2"
  }
}
